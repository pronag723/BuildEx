"use client";
import Link from "next/link";
import Avatar from "../../../lib/ui/Avatar";
import { formatPrice } from "../../../lib/pricing";
export default function ReadyBuildCard({ listing, animationDelay = 0 }) { const media=[...(listing.media||[])].sort((a,b)=>a.position-b.position); const [hero] = media; return <Link href={`/build?id=${encodeURIComponent(listing.id)}`} className="offer-card glass rounded-3xl overflow-hidden group flex flex-col" style={{animationDelay:`${animationDelay}ms`}}><div className="relative h-64 overflow-hidden bg-black/30">{hero ? <img src={hero.url} alt={hero.alt||listing.title} className="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"/> : <div className="h-full flex items-center justify-center text-gray-500">Photos coming soon</div>}<div className="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent"/><span className="absolute top-4 left-4 px-3 py-1 rounded-full bg-black/35 backdrop-blur border border-white/15 text-xs capitalize">{listing.style}</span><p className="absolute bottom-4 right-4 text-[#4ade80] font-extrabold text-lg">{formatPrice(listing.price_kopecks)}</p></div><div className="p-5"><h2 className="font-bold text-lg truncate">{listing.title}</h2><p className="text-sm text-gray-400 line-clamp-2 mt-1 min-h-10">{listing.description}</p><div className="mt-4 pt-4 border-t border-white/10 flex items-center gap-2.5"><Avatar src={listing.builder?.avatar_url} name={listing.builder?.display_name} className="w-8 h-8 rounded-full"/><span className="text-xs font-semibold truncate">{listing.builder?.display_name || listing.builder?.username}</span><span className="ml-auto text-[10px] uppercase text-gray-500">{listing.builder?.builder?.rank || "rookie"}</span></div></div></Link>; }
